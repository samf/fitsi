// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "fitsi",
    targets: [
        .executableTarget(
            name: "fitsi",
            path: "fitsi"
        )
    ]
)
